{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug",
      "type": "node-terminal",
      "request": "launch",
      "command": "npx --NODE_OPTIONS=--inspect next dev",
      "serverReadyAction": {
        "pattern": "started server on .+, url: (https?://.+)",
        "uriFormat": "%s",
        "action": "openExternally"
      }
    }
  ],
  "compounds": []
}
